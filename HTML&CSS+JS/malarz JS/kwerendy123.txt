1.
SELECT `cena`,`pojemnosc` FROM `farby` WHERE farby.kolor="czerwony";
2.
SELECT kolor , TRUNCATE((cena/pojemnosc),2) AS cena_jednostkowa FROM `farby`;
3.
SELECT kolor, liczba_puszek FROM `farby` INNER JOIN malowanie on farby.id_farby=malowanie.id_farby;
4.
CREATE VIEW pomieszczenie1 AS SELECT malowanie.id_sciany, malowanie.id_farby, malowanie.liczba_puszek from malowanie where malowanie.id_pomieszczenia="1";